/**
 * File:
 *   cfg_samba.scr
 * Summary:
 *   SCR Agent for reading/writing /etc/sysconfig/samba
 *   using the ini-agent
 * Access:
 *   read/write
 * Authors:
 *   Carsten Hoeger <choeger@suse.de>
 * See:
 *   yast2-agent-ini/ini.html
 *   libscr
 * Example:
 *   Read(.sysconfig.samba.SOMETHING)
 *   ("no")
 **
 *   Write(.sysconfig.samba.SOMETHING, "yes")
 *   (true)
 *
 * $Id$
 *
 * Read/Sets the values defined in <tt>/etc/sysconfig/samba</tt>
 * (formerly <tt>/etc/rc.config.d/samba.rc.config</tt>)
 * in an easy manner.
 */
.sysconfig.samba

`ag_ini(
  `SysConfigFile("/etc/sysconfig/samba")
)
