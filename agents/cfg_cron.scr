/**
 * File:
 *   cfg_cron.scr
 * Summary:
 *   SCR Agent for reading/writing /etc/sysconfig/cron
 *   using the ini-agent
 * Access:
 *   read/write
 * Authors:
 *   Carsten Hoeger <choeger@suse.de>
 * See:
 *   yast2-agent-ini/ini.html
 *   libscr
 * Example:
 *   Read(.sysconfig.cron.SOMETHING)
 *   ("no")
 **
 *   Write(.sysconfig.cron.SOMETHING, "yes")
 *   (true)
 *
 * $Id$
 *
 * Read/Sets the values defined in <tt>/etc/sysconfig/cron</tt>
 * (formerly <tt>/etc/rc.config.d/cron.rc.config</tt>)
 * in an easy manner.
 */
.sysconfig.cron

`ag_ini(
  `SysConfigFile("/etc/sysconfig/cron")
)
