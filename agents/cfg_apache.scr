/**
 * File:
 *   cfg_apache.scr
 * Summary:
 *   SCR Agent for reading/writing /etc/sysconfig/apache
 *   using the ini-agent
 * Access:
 *   read/write
 * Authors:
 *   Carsten Hoeger <choeger@suse.de>
 * See:
 *   yast2-agent-ini/ini.html
 *   libscr
 * Example:
 *   Read(.sysconfig.apache.SOMETHING)
 *   ("no")
 **
 *   Write(.sysconfig.apache.SOMETHING, "yes")
 *   (true)
 *
 * $Id$
 *
 * Read/Sets the values defined in <tt>/etc/sysconfig/apache</tt>
 * (formerly <tt>/etc/rc.config.d/apache.rc.config</tt>)
 * in an easy manner.
 */
.sysconfig.apache

`ag_ini(
  `SysConfigFile("/etc/sysconfig/apache")
)
